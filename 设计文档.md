# 单位行为任务链 (UGS.UnitTask) 设计文档

## 1. 背景与目标
在策略/SLG 游戏中，单位（Unit）往往需要持续执行一系列行为：移动、采集、建造、攻击、驻守、巡逻等。这些行为通常具备以下共性：

- 行为天然串行（任务链），但多个单位之间需要全局调度（优先级、资源竞争、可执行者选择）。
- 某些任务必须指定执行单位（例如“1号工人去修理”），某些任务只关心“找一个能做的人来做”（例如“任意空闲工人去采集”）。
- 任务需要优先级（紧急防御 > 正常采集），并支持常态化执行（例如“巡逻”、“维持生产线”需要循环）。

UGS.UnitTask 旨在提供一个**轻量、可扩展、可数据驱动**的“单位行为任务链”运行时框架，以 Unity Package（UPM）形式交付。

### 1.1 设计目标
- 提供任务（Task）与任务链（Chain）的抽象，并支持“指定执行单位”与“未指定执行者（运行时选择可执行者）”两种模式。
- 提供全局调度器（Scheduler），按优先级选择任务链/任务推进执行。
- 提供常态化（循环执行）机制：任务链可循环、任务可重复、可带间隔与重入策略。
- 保持底层与业务解耦：不内置具体“移动/攻击”实现，通过接口扩展。

### 1.2 非目标
- 不提供完整 AI 决策（UtilityAI/BT 等），该包只负责“任务链的运行与调度”。上层可用任意决策系统生成任务链。
- 不强依赖 GameFramework/ECS；只提供可被它们集成的运行时 API。

## 2. 核心概念

### 2.1 Unit（单位）
本包不定义具体单位类型，只依赖最小接口：

```csharp
public interface IUnit
{
    int Id { get; }
}
```

上层可用 Entity、MonoBehaviour、ECS Entity 适配成 IUnit。

### 2.2 Task（任务）
任务表示一个可被推进执行的“行为节点”。任务具备：

- **优先级（Priority）**：影响调度顺序。
- **执行者绑定策略（Executor Binding）**：
  - 绑定：任务创建时即指定执行单位（例如指定某个工人）。
  - 非绑定：任务创建时不指定单位，执行时从候选集合中选择“一个可执行者”。
- **执行状态**：任务在 Update/Tick 中推进，支持跨帧运行。

建议状态枚举：

```csharp
public enum UnitTaskStatus
{
    Pending,    // 已入队但尚未满足执行条件（含未分配执行者）
    Ready,      // 可执行
    Running,    // 执行中
    Succeeded,  // 成功
    Failed,     // 失败（可按策略重试/跳过）
    Cancelled,  // 被取消
    Skipped     // 被跳过（例如不可满足执行者/条件）
}
```

### 2.3 TaskChain（任务链）
任务链表示单位或系统希望“按顺序完成的一组任务”。任务链具备：

- 链级优先级：用于调度器在多条链之间做选择。
- 链内顺序：默认串行推进当前任务；可扩展并行/分支（本包初期建议只做串行）。
- 链级循环策略（常态化）：链完成后是否重新开始、如何等待间隔、失败如何处理。

### 2.4 Scheduler（调度器）
调度器负责在同一时间存在多条任务链、多单位竞争资源时，选择“本帧推进哪些任务/哪些链”。核心职责：

- 根据优先级选择要推进的任务链与任务。
- 为“未绑定执行者的任务”做**可执行者选择**。
- 限制每帧预算（最大推进次数/最大耗时），避免卡顿。

## 3. 执行者选择（指定 / 随机可执行者）

### 3.1 绑定执行者（指定单位）
任务定义时携带 `ExecutorUnitId`（或直接携带 IUnit 引用/句柄）。调度器仅在该单位可执行时推进该任务。

适用场景：
- 玩家明确指派
- 任务强依赖单位携带的物品/技能

### 3.2 非绑定执行者（运行时选择）
任务不携带执行单位，而是提供：

- 候选集合来源（Candidate Source）：例如“所有属于阵营 A 的工人单位”。
- 可执行判定（Eligibility）：例如“空闲、距离足够近、有采集工具”。
- 选择策略（Selection Policy）：例如随机、最近、评分最高。

为保证可测试与可复现，随机应可注入：

```csharp
public interface IRandom
{
    int NextInt(int minInclusive, int maxExclusive);
}
```

选择策略建议内置：
- RandomEligible：从可执行者里随机挑一个
- NearestEligible：按距离最近（需要上层提供距离/路径代价）
- HighestScoreEligible：按上层提供评分函数

## 4. 优先级模型

### 4.1 任务优先级 vs 链优先级
建议同时存在两层优先级：

- **链优先级（ChainPriority）**：决定“先服务哪个队列/系统意图”（例如防御链优先于采集链）。
- **任务优先级（TaskPriority）**：决定同一链内的可执行任务/候选任务排序（或在全局扁平化调度时使用）。

初期实现建议采用：
- 调度器先按 ChainPriority 选链，再推进链头当前任务（Head-of-line），避免链内乱序带来的心智复杂度。
- 若要支持“链内插队”，由上层显式操作：在链头插入高优先级任务（例如紧急撤退）。

### 4.2 调度策略（建议默认）
默认调度策略：
- 每帧循环 N 次（预算），每次选择当前最高优先级且可推进的链。
- 对该链的当前任务：
  - 若未分配执行者：尝试分配（绑定/非绑定）。
  - 若 Ready/Running：调用任务推进接口（Tick）。
  - 若 Succeeded/Failed/Skipped：按链策略移动到下一任务或结束/循环。

## 5. 常态化（循环执行）模型

### 5.1 链循环策略
链级策略建议：

```csharp
public enum ChainLoopMode
{
    OneShot,       // 执行完即结束
    Loop,          // 执行完从头开始
    LoopWithDelay  // 执行完等待一段时间再从头开始
}
```

并配置：
- LoopDelaySeconds（可选）
- OnTaskFailedPolicy：失败时是中止链、跳过任务继续、重试若干次、退避重试等

### 5.2 任务重复策略（可选增强）
部分任务本身就是“持续任务”（例如巡逻、维持驻守）。建议通过以下方式表达：
- 任务返回 Running 持续推进，直到上层条件满足才 Succeeded（例如巡逻达到目标点后继续下一段）
- 或提供“RepeatTaskDecorator”：任务完成后按策略重置并再次进入 Pending/Ready

## 6. 运行时接口草案（面向实现）

### 6.1 任务接口

```csharp
public interface IUnitTask
{
    int Priority { get; }
    UnitTaskStatus Status { get; }

    // 绑定执行者：可能为空表示未绑定
    int? BoundUnitId { get; }

    // 调度器调用：尝试分配执行者（对非绑定任务生效）
    bool TryBindExecutor(IUnitTaskContext context);

    // 调度器调用：推进执行（可跨帧）
    void Tick(IUnitTaskContext context, float deltaTime);

    // 可选：取消/重置
    void Cancel(IUnitTaskContext context);
    void Reset(IUnitTaskContext context);
}
```

### 6.2 上下文接口（由宿主游戏提供）
上下文用于解耦底层与具体游戏系统：

```csharp
public interface IUnitTaskContext
{
    bool TryGetUnit(int unitId, out IUnit unit);
    IReadOnlyList<int> GetCandidateUnitIds(UnitTaskCandidateQuery query);
    bool IsUnitEligible(int unitId, IUnitTask task);
    IRandom Random { get; }
    float Time { get; }
}
```

其中 `UnitTaskCandidateQuery` 由上层定义筛选条件（阵营、类型、标签等）。

### 6.3 任务链接口

```csharp
public interface IUnitTaskChain
{
    int ChainId { get; }
    int Priority { get; }
    ChainLoopMode LoopMode { get; }

    UnitTaskChainStatus Status { get; }
    IUnitTask? Current { get; }

    void Enqueue(IUnitTask task);
    void Tick(IUnitTaskContext context, float deltaTime);
    void Cancel(IUnitTaskContext context);
}
```

任务链状态建议：

```csharp
public enum UnitTaskChainStatus
{
    Active,
    Paused,
    Completed,
    Cancelled
}
```

### 6.4 调度器接口

```csharp
public interface IUnitTaskScheduler
{
    void AddChain(IUnitTaskChain chain);
    bool RemoveChain(int chainId);
    void Tick(IUnitTaskContext context, float deltaTime);
}
```

调度器可提供配置：
- MaxChainsPerTick（每帧最多推进多少条链）
- MaxTasksPerTick（每帧最多推进多少个任务）
- 可选：时间预算（毫秒）+ Stopwatch

## 7. 数据与序列化（建议方案）
为适配策略游戏常见的“配置驱动”，建议支持以下两条路径：

- **纯运行时构建**：上层根据 AI/玩家操作动态创建任务实例并组链。
- **SerializeReference 数据驱动**：任务定义以 `SerializeReference` 持久化（例如存档/回放）。每种任务实现为可序列化的类（避免 ScriptableObject 过多实例化开销）。

存档建议以“任务定义 + 运行态最小数据（状态/进度/已绑定单位）”形式存储，具体序列化由宿主系统决定（JSON/MemoryPack/Protobuf 等）。

## 8. 包结构（UPM）
建议目录：

```text
UGS.UnitTask/
├── package.json
├── Runtime/
│   ├── Core/
│   ├── Scheduling/
│   ├── Selection/
│   ├── Models/
│   └── UGS.UnitTask.Runtime.asmdef
├── Editor/ (可选)
├── Tests/ (可选)
└── 设计文档.md
```

初期仅需要 Runtime + 文档；Editor/Tests 可在实现阶段补齐。

## 9. 典型流程示例

### 9.1 指定单位执行（建造）
1. UI 选择工人 A，对目标建筑点下达“建造”任务（绑定 A）。
2. 任务链加入 `MoveTo(buildSite)` → `Build(buildSite)`。
3. 调度器按优先级推进该链，直到完成。

### 9.2 未指定单位执行（采集）
1. 系统产生任务 `Gather(resourceNode)`（不绑定单位）。
2. 调度器在 Tick 中查询候选工人集合，筛选可执行者，按 RandomEligible 分配一个工人。
3. 任务链开始执行 `MoveTo(resourceNode)` → `Gather(resourceNode)`，完成后链可按 LoopWithDelay 继续寻找下一资源点。

## 10. 实现注意点（落地约束）
- 任务与链的推进应尽量无 GC：避免 LINQ、避免频繁分配临时集合；候选列表可复用缓冲区或由上层提供。
- 随机选择应支持可复现（注入 IRandom）。
- 调度器不要隐式“抢占”单位；若需要资源锁（单位忙碌/占用），通过 Context 的 IsUnitEligible 与上层的占用系统配合实现。

## 11. 可视化调试（Visual Debugging）
任务链调度类系统的排查难点通常在于“为什么这个单位没去做事/为什么链卡住/为什么被别的任务抢占”。因此建议在包设计中预留可视化调试能力，且做到：

- 默认不影响运行时代码尺寸与性能（通过编译宏或可插拔 Debug Provider 开关）。
- 能在 Editor 下快速定位“链/任务/单位”的实时状态。
- 支持录制与回放关键决策（候选集、选择策略、失败原因），便于复现。

### 11.1 调试目标
- 全局视角：当前有哪些任务链、各自优先级、循环模式、当前任务与状态。
- 单位视角：单位当前绑定了哪些任务/被哪些链占用、最近一次绑定/解绑定原因。
- 选择过程：非绑定任务在分配执行者时的候选集合、过滤原因、最终选择策略输出。
- 卡住诊断：任务处于 Pending/Ready 但无法进入 Running 的原因（例如无候选、单位不满足资格、链被暂停、预算限制）。

### 11.2 运行时数据快照（Snapshot）
建议调度器提供一个只读快照接口，供调试 UI 拉取：

```csharp
public interface IUnitTaskDebugSnapshotSource
{
    UnitTaskSchedulerSnapshot Capture();
}
```

快照建议包含（只读结构体/POCO）：
- Chains：ChainId、Priority、LoopMode、Status、CurrentTask、QueueLength
- Tasks：TaskId（或类型名）、Priority、Status、BoundUnitId、Progress（可选）、LastError（可选）
- Units：UnitId、IsBusy（若上层提供）、CurrentChainId/CurrentTaskId（若可推导）
- Decisions：最近 N 条“分配执行者/推进/失败/跳过”的决策记录（可选环形缓冲）

### 11.3 Editor 调试窗口（建议形态）
建议提供一个 EditorWindow（UI Toolkit 或 IMGUI 均可）用于实时查看：

- 左侧链列表：支持按 Priority、Status、LoopMode 过滤/排序。
- 中间链详情：显示任务队列（含状态颜色）、当前任务的关键字段（BoundUnitId、重试次数等）。
- 右侧单位视图：展示单位列表或搜索 UnitId，并显示其当前绑定任务/占用来源。

交互建议：
- Pause/Resume：暂停调度器 Tick（仅调试模式可用）。
- Step：单步推进一次 Tick（或推进指定链一次）。
- Pin/Follow：锁定某个 ChainId/UnitId，便于观察。
- Export：导出最近决策日志为文本（用于粘贴到 issue/IM）。

### 11.4 场景叠加（Gizmos/Handles）
若宿主游戏可提供空间信息（位置、目标点），可在 Scene 视图绘制：
- 绑定关系：Unit → Task 目标的连线与标签
- 链状态：在单位头顶显示当前任务名/状态/剩余次数

为了保持解耦，包内不直接依赖 Transform/Vector3；由宿主通过可选接口提供坐标查询：

```csharp
public interface IUnitSpatialDebugProvider
{
    bool TryGetWorldPosition(int unitId, out UnityEngine.Vector3 position);
}
```

### 11.5 诊断码（Reason Codes）
为了让可视化调试“可解释”，建议在以下关键点产出标准化原因码：
- 非绑定任务无法分配执行者：NoCandidates / AllIneligible / BudgetLimited / SelectionError
- 绑定任务无法推进：UnitNotFound / UnitIneligible / TaskPreconditionFailed
- 任务失败：TaskExecutionFailed（可携带业务侧错误码字符串）

建议用枚举或短字符串表达，避免复杂异常链带来的 GC。

### 11.6 编译与开关策略
建议提供宏开关与运行时开关两层：
- 编译宏：`UGS_UNITTASK_DEBUG`（决定是否编译调试窗口/决策记录）
- 运行时开关：SchedulerConfig.EnableDebugTrace（决定是否采集决策环形缓冲）

默认关闭调试采集，仅在需要时开启。

