# UGS.UnitTask 分阶段实现文档

本文档用于将 UGS.UnitTask（单位行为任务链）按可交付、可验证的方式分阶段落地，实现过程中优先保证：接口稳定、可扩展、可调试、低 GC、易集成。

## 阶段 0：包骨架与最小可运行（MVP）
**目标**：在 Unity 工程中以 UPM 包形式导入后，能跑通“创建任务链 → 调度 Tick → 任务推进 → 链完成”的最短路径。

**交付内容**
- UPM 基础结构：`package.json`、`Runtime/`、`UGS.UnitTask.Runtime.asmdef`
- 核心模型与接口：
  - `IUnit`
  - `UnitTaskStatus`
  - `IUnitTask`（含 Priority、Status、BoundUnitId、TryBindExecutor、Tick、Cancel、Reset）
  - `IUnitTaskContext`（TryGetUnit / 候选查询 / Eligibility / Random / Time）
  - `IUnitTaskChain`（串行队列）
  - `IUnitTaskScheduler`（按链优先级推进链头任务）
- 1 个最小可验证任务实现：
  - `WaitSecondsTask`（不依赖空间信息，便于单元测试）

**验收标准**
- 能同时跑多条链，调度器按 Priority 选择推进
- 任务可跨帧 Running，并最终 Succeeded
- 取消链/任务后状态一致且不会继续推进

## 阶段 1：执行者绑定与选择策略
**目标**：完整支持“指定执行单位”与“未指定执行者（运行时选择）”，并具备可解释的失败原因。

**交付内容**
- 执行者绑定规则落地：
  - 绑定任务：仅当 BoundUnitId 单位存在且 Eligible 才推进
  - 非绑定任务：在 Pending/Ready 阶段尝试分配执行者
- 候选选择模块（Selection）：
  - RandomEligible（必做）
  - NearestEligible（可选，依赖宿主提供距离/代价）
  - HighestScoreEligible（可选，依赖宿主评分）
- 诊断原因码（Reason Codes）：
  - NoCandidates / AllIneligible / UnitNotFound / BudgetLimited / SelectionError / TaskPreconditionFailed

**验收标准**
- 非绑定任务在候选集合变化后可以重新绑定并推进
- 当无法绑定/无法推进时能得到明确原因码（可用于调试 UI）

## 阶段 2：循环执行（常态化）与失败策略
**目标**：支持 OneShot/Loop/LoopWithDelay，且对失败具备可配置的链级策略。

**交付内容**
- `ChainLoopMode` 与循环参数：
  - LoopDelaySeconds（可选）
- 链级失败策略（建议最小集合）：
  - AbortChain（失败即结束）
  - SkipTaskAndContinue（跳过失败任务继续）
  - RetryTask（重试 N 次，可选指数退避）
- 链运行状态：
  - Active / Paused / Completed / Cancelled

**验收标准**
- LoopWithDelay 的延迟不产生额外 GC（避免频繁分配计时器对象）
- Retry 不会导致无穷重试（有上限或可中止）

## 阶段 3：可视化调试（第一版：快照 + 追踪）
**目标**：不做 UI 也能调试——通过快照与环形缓冲追踪，把“为什么没执行”问题定位到链/任务/选择层面。

**交付内容**
- 快照：
  - `IUnitTaskDebugSnapshotSource.Capture()`
  - `UnitTaskSchedulerSnapshot`（Chains/Tasks/Decisions）
- 调试追踪（可开关）：
  - 决策环形缓冲（最近 N 条：绑定/推进/失败/跳过）
  - 编译宏：`UGS_UNITTASK_DEBUG`
  - 运行时开关：`EnableDebugTrace`

**验收标准**
- 关闭调试采集时：零分配或接近零分配（不写入决策记录）
- 开启调试采集时：每次决策写入 O(1)（环形数组）且无额外临时分配

## 阶段 4：Editor 可视化调试窗口（第二版：UI）
**目标**：在 Unity Editor 中可实时观察链/任务/单位关系，并支持 Pause/Step/Follow。

**交付内容**
- Editor 程序集：`Editor/` + `UGS.UnitTask.Editor.asmdef`
- EditorWindow（UI Toolkit 或 IMGUI 二选一）：
  - 链列表（过滤：Priority/Status/LoopMode）
  - 链详情（队列、当前任务字段、原因码）
  - 单位视图（按 UnitId 搜索，显示绑定信息）
- 调试交互：
  - Pause/Resume（调度器 Tick 外置控制或注入时间）
  - Step（单步 Tick 或单步推进指定链）
  - Export（导出最近决策记录为文本）

**验收标准**
- 窗口开启不会显著影响 Play 模式性能（除非主动开启 DebugTrace）
- 能稳定定位某条链/某个单位并持续跟踪

## 阶段 5：序列化/存档与回放支持
**目标**：让任务链可以持久化（存档）与复现（回放/重现 bug）。

**交付内容**
- 最小存档模型：
  - Chain：ChainId、Priority、LoopMode、当前索引/队列、循环/延迟状态、失败策略状态（重试计数等）
  - Task：类型标识、Priority、Status、BoundUnitId、任务自定义进度数据（由任务实现提供）
- 任务自定义状态接口（建议）：
  - `IUnitTaskStateSerializable`（导出/导入 POCO）
- 与宿主序列化工具解耦：
  - 不强依赖 JSON/MemoryPack/Protobuf，由宿主调用导出/导入结果

**验收标准**
- 存档后恢复，链/任务继续推进且结果一致
- 在相同随机种子下（IRandom 可注入），回放能复现关键选择结果

## 阶段 6：测试、性能与集成示例
**目标**：保证演进过程中接口稳定与行为可回归，并提供最小集成示例降低接入成本。

**交付内容**
- Tests（EditMode 或 PlayMode）：
  - 调度优先级顺序
  - 绑定/非绑定执行者选择
  - 循环/失败策略
  - 快照/追踪的稳定性
- 性能基线：
  - 关键路径避免 LINQ，避免装箱/闭包分配
  - 可选预算（MaxChainsPerTick / MaxTasksPerTick / 时间预算）
- 示例（Samples~ 可选）：
  - 2-3 个单位 + 2 条链（建造链、采集链），展示优先级抢占与循环

**验收标准**
- 核心回归测试覆盖关键行为
- 在常见规模（例如 1k 链、5k 单位的候选查询由宿主提供）下可稳定运行且无明显 GC 峰值

## 风险与约束（实现中需持续校验）
- 候选查询与 Eligibility 通常是最重的部分：必须允许宿主实现高性能查询（缓存/空间索引/ECS Query）。
- “单位占用/忙碌”不应由调度器暗中管理：通过 Context 的 Eligibility 与宿主占用系统协作，避免双重状态。
- Editor 调试与运行时逻辑必须分程序集隔离，避免把 Editor 依赖带入 Runtime。

